import { EventEmitter } from '../../stencil-public-runtime';
declare global {
  interface Window {
    ethereum: any;
  }
}
export declare class NewUser {
  isAccountDisconnected: boolean;
  buttonClass: string;
  community: any;
  isPartner: Boolean;
  showUserDetails: EventEmitter<Boolean>;
  componentWillLoad(): void;
  handleMetamaskClick: () => Promise<void>;
  handleUserDetailsClick(): void;
  render(): any;
}
