import { EventEmitter } from '../../stencil-public-runtime';
export declare class UsersModal {
  isLoading: Boolean;
  isPartner: Boolean;
  showNewScreen: EventEmitter<any>;
  closeModalOnLogin: EventEmitter<any>;
  handleNewScreen(text: any): void;
  handleMetamaskClick: () => Promise<void>;
  render(): any;
}
